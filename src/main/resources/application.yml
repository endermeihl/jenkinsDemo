spring:
  application:
    name: eureka-server
server:
  port: 1001
  servlet:
    context-path: /
eureka:
  instance:
    appname: localhost
  client:
    register-with-eureka: false
    fetch-registry: false
    serviceUrl:
      defaultZone: http://localhost:1001/eureka/